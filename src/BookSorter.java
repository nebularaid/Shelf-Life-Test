import java.util.ArrayList;

public class BookSorter {
    private ArrayList<Book> list;

    public BookSorter(ArrayList<Book> )
    public void sortByDateRead(ArrayList<Book> list) {

    }
    private int earliestFromIndex(ArrayList<Book> list, int index) {
        for (int i = index; ) {

        }
    }

    private void swap() {

    }
}
